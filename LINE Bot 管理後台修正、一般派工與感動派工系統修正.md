# 程式碼修訂報告 (2025-12-29)

本文件彙整了今日針對 LINE Bot 管理後台、一般派工與感動派工系統的所有修訂內容。本日重點在於整合雙軌任務數據顯示、優化導航流程及修復資料對應問題。

## 1. 前端介面優化 (Frontend)

### 1.1 LINE Bot 管理儀表板 (`templates/linebot_dashboard.html`)
*   **雙軌數據顯示**：
    *   **一般任務與感動任務並列**：每個部門卡片現在同時顯示兩組統計數據。
        *   **一般任務**：顯示 `總任務` | `待處理` | `處理中` | `已完成`。
        *   **感動任務**：顯示 `總任務` | `未執行` | `執行中` | `已完成`。
    *   **互動式導航**：統計區塊新增點擊功能。
        *   點擊「一般任務」區塊 -> 跳轉至一般派工列表並自動篩選該部門。
        *   點擊「感動任務」區塊 -> 跳轉至感動派工列表並自動篩選該部門。
*   **版面配置調整**：
    *   **4x3 網格排版**：大型螢幕 (>=1200px) 強制 4 欄排版。
    *   **容器加寬**：最大寬度提升至 `1400px`。
    *   **自動刷新**：設定為 10 分鐘 (600秒) 自動更新，並附帶倒數計時器。

### 1.2 側邊選單 (`templates/sidebar.html`)
*   **新增入口**：於「派工管理」區塊新增 **「LINE Bot 管理中心」** 選項，並置於首位，方便管理員快速存取。

### 1.3 任務列表頁面 (`templates/tasks.html`, `templates/emotional_tasks.html`)
*   **部門篩選支援**：
    *   新增 URL 參數 `dept` 的支援 (例如 `?dept=GS`)。
    *   當從儀表板跳轉過來時，列表會自動過濾出特定部門的任務。

## 2. 後端核心邏輯修正 (Backend)

### 2.1 LINE Bot 服務層 (`app/services/linebot_service.py`)
*   **統計數據邏輯重構**：
    *   `get_task_statistics` 函式現在會同時查詢 `Task` (一般任務) 與 `EmotionalTask` (感動任務) 兩張資料表。
    *   **部門代碼對照 (Mapping)**：解決了儀表板代碼 (如 `GS`) 與一般任務資料表全名 (如 `front_office`) 不一致的問題，確保數據能正確撈取。
*   **API 容錯處理**：
    *   針對 Flex Message 的 `content`, `time`, `remark` 欄位加入空值檢查，防止 LINE API 報錯 (400 Bad Request)。

## 3. 資料庫與數據修復 (Database & Data)

*   **FB 部門代碼清洗**：
    *   修正 `emotional_tasks` 資料表中，餐飲部誤植為 `'F&B'` 的問題，統一更新為 `'FB'`。
*   **一般任務資料對應**：
    *   確認一般任務資料表使用英文全名 (如 `housekeeping`) 作為部門識別，已在後端與前端加入對照表機制以支援縮寫查詢。

---
**操作提醒**：
所有後端 Python 程式碼 (`.py` 檔案) 的變更，皆需要**重新啟動 FastAPI 伺服器** (`python run.py`) 才會生效。前端 HTML/JS 的變更則只需重新整理網頁即可。
