# 感動派工列表頁面建置完成報告

## 建置日期
2025年12月27日

## 完成項目

### 1. 後端程式碼

#### a. 視圖層 (View)
- **檔案**: `app/views/emotional_task_view.py`
- **功能**: 處理感動派工列表頁面的渲染
- **類別**: `EmotionalTaskView`
- **方法**: `emotional_task_list_page()` - 渲染頁面

#### b. 路由層 (Routes)
- **檔案**: `app/routes/emotional_task_routes.py`
- **路徑前綴**: `/api/emotional-tasks`
- **端點**:
  - `GET /api/emotional-tasks/` - 取得所有感動派工任務
  - `GET /api/emotional-tasks/emergency` - 取得緊急感動派工任務
  - `PATCH /api/emotional-tasks/{task_id}/status` - 更新任務狀態

#### c. 主程式整合
- **檔案**: `run.py`
- **更新內容**:
  - 引入 `emotional_task_router` 和 `emotional_task_view`
  - 註冊 API 路由: `app.include_router(emotional_task_router, prefix="/api")`
  - 註冊頁面路由: `@app.get("/emotional-tasks")`

### 2. 前端頁面

#### a. HTML 模板
- **檔案**: `templates/emotional_tasks.html`
- **功能**: 完整的感動派工列表展示頁面
- **欄位**:
  - 任務代碼 (task_id)
  - 部門名稱 (dept_name)
  - 執行日期 (task_date)
  - 地點代碼 (location_code)
  - 優先次序/執行階段 (sequence_stage: P/E/F → 準備/執行/收尾)
  - 任務標題 (task_title)
  - 任務內容 (action_item)
  - 備註 (note)
  - 任務狀態 (pending/assigned/in_progress/completed/cancelled)
  - 預定時間 (start_time ~ end_time)
  - 完成時間 (completed_at；若無則顯示 updated_at)
  - 操作按鈕

#### b. 頁面功能
- ✅ 狀態篩選 (pending/assigned/in_progress/completed/cancelled)
- ✅ 重新整理按鈕
- ✅ 分頁顯示 (每頁 10 筆)
- ✅ 分頁控制項 (上一頁/下一頁)
- ✅ 響應式表格設計
- ✅ 查看任務詳情功能

#### c. 側邊選單更新
- **檔案**: `templates/sidebar.html`
- **更新內容**: 
  - 將「感動派工單列表」連結更新為 `/emotional-tasks`
  - 更改圖示為 💝

### 3. 頁面樣式

所有樣式已完整複製自派工單列表頁面，包括：
- 暗色主題設計
- 狀態標籤顏色 (待處理/已派工/處理中/已完成)
- 篩選欄樣式
- 分頁控制項樣式
- 表格響應式設計

## 目前狀態

### ✅ 已完成

#### 資料庫設定與模型
1. 建立 EmotionalTask ORM 模型 `app/models/emotional_task.py`
   - 映射到 MySQL `ace` 資料庫的 `emotional_tasks` 表
   - 包含 16 個欄位：id、project_code、task_id、dept_code、dept_name、task_date、time_start、time_end、location_code、sequence_stage、task_title、action_item、note、status、completed_at、created_at、updated_at
   - task_id 設為唯一索引，支援 upsert 邏輯

2. 建立 EmotionalTaskResponse Pydantic Schema `app/schemas/emotional_task_schema.py`
   - 實現 ORM→JSON 序列化
   - 從 ORM 模型自動提取屬性（from_attributes=True）

3. 實作 API 路由 `app/routes/emotional_task_routes.py`：
   - **_seed_emotional_tasks()**: 從 db/ 目錄中的 JSON 檔自動匯入任務資料
     - 支援多個 JSON 檔（運動鞋製造公司_主管大會、親子兩天一夜_旅程）
     - 自動解析日期偏移（day 欄位）轉換為實際執行日期
     - 實現 upsert 邏輯：task_id 既存則更新，不存在則新增
   - **GET /api/emotional-tasks/**: 回傳所有任務，依日期時間排序，支援 response_model 序列化
   - **GET /api/emotional-tasks/emergency**: 回傳待處理、已指派、進行中的任務
   - **PATCH /api/emotional-tasks/{task_id}/status**: 更新任務狀態，自動更新 updated_at 時間戳

4. 資料庫配置更新 `app/core/config.py`
   - DATABASE_URL 改為 `mysql+pymysql://root:1234@localhost:3306/ace`（從 tibame_ace_db 變更）

5. 主程式整合 `run.py`
   - 引入 `app.models.emotional_task` 確保 SQLAlchemy 在啟動時建表
   - 已測試資料匯入：55 筆任務成功載入（VIC 專案 29 筆 + HLA 專案 26 筆）

#### 前端調整
1. 表頭欄位對齊資料庫結構
   - 任務代碼 (task_id)
   - 部門名稱 (dept_name)
   - 執行日期 (task_date)
   - 地點代碼 (location_code)
   - 優先次序 (sequence_stage → P/E/F)
   - 任務標題 (task_title)
   - 任務內容 (action_item)
   - 備註 (note)
   - 任務狀態 (status)
   - 預定時間 (time_start~time_end)
   - 完成時間 (completed_at)
   - 操作列表

2. 優先次序顯示修正
   - 從中文翻譯 (準備/執行/收尾) 改為英文代號顯示 (P/E/F)
   - 直接取用資料庫中的 sequence_stage 欄位值

3. 預定時間格式化
   - 新增 formatTime() JavaScript 函數，將時間物件轉換為 HH:MM 格式
   - 時間區間顯示為 HH:MM~HH:MM 格式（無空格）
   - 例：09:00~10:00

4. 完成時間邏輯修正
   - 移除更新時間 (updated_at) 的備用顯示
   - 完成時間僅顯示 LineBot 回傳的 completed_at（無則顯示 -）

5. 狀態篩選對應
   - pending → 待處理 (黃色)
   - assigned → 已指派 (藍色)
   - in_progress → 進行中 (綠色)
   - completed → 已完成 (灰色)
   - cancelled → 已取消 (灰色)

## 資料庫結構

### emotional_tasks 表欄位

| 欄位 | 型態 | 說明 | 備註 |
|------|------|------|------|
| id | BIGINT | 自動遞增主鍵 | PK, AUTO_INCREMENT |
| project_code | VARCHAR(10) | 專案代碼 | VIC（運動鞋）、HLA（旅程） |
| task_id | VARCHAR(20) | 任務代碼 | UNIQUE，例：VIC-AD-101 |
| dept_code | VARCHAR(10) | 部門代碼 | AD、HK、GS、KT 等 |
| dept_name | VARCHAR(50) | 部門名稱 | 美術設計部、房務部等 |
| task_date | DATE | 執行日期 | 自動由 day 偏移計算 |
| time_start | TIME | 開始時間 | 執行開始時間，格式 HH:MM |
| time_end | TIME | 結束時間 | 執行結束時間，格式 HH:MM |
| location_code | VARCHAR(20) | 地點代碼 | L01、B01、S1205 等 |
| sequence_stage | VARCHAR(5) | 執行階段 | P=準備、E=執行、F=收尾 |
| task_title | VARCHAR(100) | 任務標題 | 主要任務名稱 |
| action_item | LONGTEXT | 執行項目 | 詳細的執行描述 |
| note | LONGTEXT | 備註 | 備註與注意事項 |
| status | VARCHAR(20) | 任務狀態 | pending/assigned/in_progress/completed/cancelled |
| completed_at | DATETIME | 完成時間 | LineBot 回傳時設定，NULL 表示未完成 |
| created_at | TIMESTAMP | 建立時間 | 資料新增時自動設定 |
| updated_at | TIMESTAMP | 更新時間 | 任何更新時自動更新 |

## 資料匯入來源

### 來源檔案
兩份 JSON 檔位於 `db/` 目錄（與 Tibame-ACE 同層級）：

1. **運動鞋製造公司_主管大會.json**
   - 專案代碼：VIC
   - 共 29 筆任務
   - 涵蓋各部門（美術設計部、房務部、工程部、企劃推廣部等）

2. **親子兩天一夜_旅程.json**
   - 專案代碼：HLA
   - 共 26 筆任務
   - 涵蓋各部門的旅程相關工作

### 資料轉換邏輯
- **日期計算**: JSON 中的 `day` 欄位為相對偏移（day 1 表示今日 + 0 天），自動轉換為實際日期
- **時間解析**: `time_start` 和 `time_end` 為 "HH:MM" 格式字串，轉換為 Python time 物件存入資料庫
- **Upsert 邏輯**: 
  - 若 `task_id` 已存在→更新記錄
  - 若 `task_id` 不存在→新增記錄
  - 資料首次匯入或新增 JSON 檔時會自動觸發

## 程式修正過程

### Phase 1: 初始化與資料庫設定
1. 建立 `app/models/emotional_task.py` - ORM 模型定義
2. 建立 `app/schemas/emotional_task_schema.py` - Pydantic 序列化 Schema
3. 更新 `app/core/config.py` - 資料庫 URL 改為 ace（從 tibame_ace_db）
4. 更新 `run.py` - 引入 emotional_task 模型

### Phase 2: API 路由實作
1. 建立 `app/routes/emotional_task_routes.py`
2. 實作 _parse_time() 函數解析 JSON 時間格式
3. 實作 _seed_emotional_tasks() 自動匯入資料
4. 實作三個主要端點（GET all、GET emergency、PATCH status）
5. 設定 response_model 確保正確的 JSON 序列化

### Phase 3: 前端調整
1. 修正表頭欄位對應資料庫欄位名稱
2. 修正 sequence_stage 顯示方式（中文→英文代號 P/E/F）
3. 修正預定時間格式（新增 formatTime() 函數，移除空格）
4. 修正完成時間邏輯（改用 completed_at，移除 updated_at 備用）

## 主要修正清單

### 修正項目 1: JSON 檔案路徑
- **問題**: 初始使用 parents[2]，導致路徑錯誤
- **修正**: 改為 parents[3]，正確定位到 Tibame-ACE 上層的 db/ 目錄
- **驗證**: 55 筆任務成功載入

### 修正項目 2: 資料序列化
- **問題**: EmotionalTask ORM 物件無法直接序列化為 JSON
- **修正**: 
  - 建立 EmotionalTaskResponse Pydantic Schema
  - 所有 GET 端點添加 response_model=List[EmotionalTaskResponse]
  - 端點內使用 [EmotionalTaskResponse.model_validate(task) for task in tasks]
- **驗證**: 返回的 JSON 包含完整的 16 個欄位

### 修正項目 3: 資料庫名稱
- **問題**: 連線至錯誤的資料庫 tibame_ace_db
- **修正**: `app/core/config.py` DATABASE_URL 改為 ace 資料庫
- **驗證**: 資料正確寫入 ace 資料庫的 emotional_tasks 表

### 修正項目 4: 優先次序顯示
- **問題**: 嘗試從 sequence_stage 翻譯中文（準備/執行/收尾）
- **修正**: 改為直接顯示資料庫值 P/E/F 代號
- **驗證**: 前端表格顯示單字母代碼

### 修正項目 5: 預定時間格式
- **問題**: 
  - 時間欄位引用錯誤 (start_time → time_start)
  - 格式包含空格 "09:00 ~ 10:00"
- **修正**:
  - 新增 formatTime() JavaScript 函數，將時間物件轉換為 "HH:MM" 格式
  - 修正欄位引用為 time_start 和 time_end
  - 移除時間區間中的空格
- **驗證**: 前端顯示 "09:00~10:00" 格式

### 修正項目 6: 完成時間邏輯
- **問題**: 原本設計中完成時間會優先顯示 completed_at，無則顯示 updated_at
- **修正**: 移除 updated_at 備用邏輯，完成時間僅顯示 LineBot 回傳的 completed_at（無則顯示 -）
- **驗證**: 尚未完成的任務 completed_at 為 NULL，顯示 -

## 資料驗證結果

### 匯入統計
- ✅ 總任務數：55 筆
  - VIC 專案：29 筆
  - HLA 專案：26 筆
- ✅ 分頁顯示：6 頁（每頁 10 筆）
- ✅ 狀態分布：全部為 pending（待處理）

### 顯示驗證
- ✅ 任務代碼：正確顯示 (VIC-AD-101 等)
- ✅ 部門名稱：正確顯示
- ✅ 執行日期：自動計算正確
- ✅ 優先次序：顯示 P/E/F 代號
- ✅ 預定時間：顯示 HH:MM~HH:MM 格式
- ✅ 狀態標籤：待處理 (黃色)
- ✅ 分頁功能：正常運作

## 使用方式

### 存取頁面
1. 啟動應用程式: `python run.py`
2. 登入後台管理系統
3. 點擊側邊選單「感動派工列表」
4. 或直接存取: `http://localhost:8000/emotional-tasks`

### API 端點測試
```bash
# 取得所有感動派工任務
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost:8000/api/emotional-tasks/

# 取得緊急感動派工任務
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost:8000/api/emotional-tasks/emergency

# 更新任務狀態
curl -X PATCH -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"status": "in_progress"}' \
  http://localhost:8000/api/emotional-tasks/VIC-AD-101/status
```

## 備註

- 所有資料已驗證並成功匯入至 ace 資料庫
- 頁面樣式與「派工單列表」保持一致
- LineBot 集成將透過 PATCH 端點更新任務狀態和完成時間
- 分頁、篩選、排序等前端功能已完整實作
